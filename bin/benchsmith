#!/usr/bin/env python3
"""benchsmith — entry point. Stdlib only; no install step."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "lib"))
from benchsmith.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
